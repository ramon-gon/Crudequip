<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph_administrador"
    app:startDestination="@id/menu">

    <fragment
        android:id="@+id/menu"
        android:name="copernic.cat.kingsleague.Menu"
        android:label="fragment_menu_admin"
        tools:layout="@layout/fragment_menu_admin" >
        <action
            android:id="@+id/action_menu_to_classificacio"
            app:destination="@id/classificacio" />
    </fragment>
    <fragment
        android:id="@+id/classificacio"
        android:name="copernic.cat.kingsleague.Classificacio"
        android:label="fragment_classificacio"
        tools:layout="@layout/fragment_classificacio" />
</navigation>